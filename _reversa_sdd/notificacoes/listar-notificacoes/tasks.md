# Listar Notificações — Tarefas

> `tasks.md` | Caso de uso: `notificacoes/listar-notificacoes`

---

- [ ] T-01 — SELECT WHERE user_id ORDER BY created_at DESC
- [ ] T-02 — Retornar `[]` sem erro se userId ausente
- [ ] TT-01 — GET com userId → notificações ordenadas
- [ ] TT-02 — GET sem userId → lista vazia
