# Criar Coluna — Tarefas de Implementação

> `tasks.md` | Caso de uso: `colunas/criar-coluna`

---

## Tarefas (fiel ao legado)

- [ ] T-01 — Implementar POST handler: `name.toUpperCase()`, retornar `{ column }` sem INSERT
  - Confiança: 🟢

---

## Tarefa de Roadmap (corrigir lacuna crítica)

- [ ] DT-01 — Substituir handler por POST real com `INSERT INTO columns`
  - Pré-requisito: tabela `columns` criada (ver `colunas/tasks.md` DT-01)
